# MagicBook 3.0 — 環境變數範例
#
# 複製此檔案為 .env.local（本機開發用，已列入 .gitignore，不會被 commit）。
#
# 依 PM 指示：
# - 不在聊天室提供 Service Role Key。
# - 不把任何 Secret / Service Key 寫入 GitHub。
# - 這裡只使用 Supabase Publishable Key（sb_publishable_...），不使用 secret / service role key。

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
